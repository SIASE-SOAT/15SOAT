apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: siase
  labels:
    app: siase-frontend
spec:
  selector:
    app: siase-frontend
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: LoadBalancer
