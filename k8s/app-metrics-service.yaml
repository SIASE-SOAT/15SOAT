apiVersion: v1
kind: Service
metadata:
  name: app-metrics
  namespace: siase
  labels:
    siase-metrics: "true"
spec:
  selector:
    app: siase-app
  ports:
    - name: management
      port: 8081
      targetPort: 8081
  type: ClusterIP
