apiVersion: v1
kind: Secret
metadata:
  name: siase-secret
  namespace: siase
type: Opaque
stringData: {}
# Os valores reais são buscados do Secrets Manager pelo workflow de produção.
# Chaves esperadas: DB_USER, DB_PASSWORD, JWT_SECRET, WEBHOOK_TOKEN e
# MECANICO_PASSWORD.
