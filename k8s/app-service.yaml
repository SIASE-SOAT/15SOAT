apiVersion: v1
kind: Service
metadata:
  name: app-service
  namespace: siase
spec:
  selector:
    app: siase-app
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
  type: NodePort
